cmake_minimum_required(VERSION 3.22)

project(
    "netmap"
    VERSION 1.0
    DESCRIPTION "A project to control the high gain antenna and provide GUI functionality."
    LANGUAGE CXX)

add_executable(netmap.exe)

target_sources()